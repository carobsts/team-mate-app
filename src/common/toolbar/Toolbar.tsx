import { FC } from 'react';
import './Toolbar.css';

const Toolbar: FC<any> = () => {
    return (
        <section className='Toolbar__container--wrapper'>
            <div>
                <p></p>
            </div>
        </section>
    )
};

export default Toolbar;