import { FC } from 'react'; 

interface PrayersListProps {};

const PrayersList: FC<PrayersListProps> = () => {
    return (
        <section>

        </section>
    )
};

export default PrayersList;